# Force LF in working trees on every OS — Prettier (endOfLine: lf) and the
# format:check CI gate fail on CRLF checkouts (Windows autocrlf).
* text=auto eol=lf
*.png binary
*.jpg binary
*.jpeg binary
